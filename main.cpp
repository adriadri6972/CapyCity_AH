#include "CapyCitySim.h"

// alle Aufgaben von Kapitel 3 habe ich ausversehen während Kapitel 2 schon erledigt 
// (bei der Optimierung während ich auf die Folgekapitel gewartet habe)
// ich verwalte meine Materialen als map über die Gebäude (Carlas Idee)


int main()
{
	CapyCitySim a = CapyCitySim();
	//CapyCitySim b = CapyCitySim();
	//CapyCitySim c = CapyCitySim();
	//CapyCitySim d = CapyCitySim();

	while (true) {
		a.menue();
	}
	return 0;
}